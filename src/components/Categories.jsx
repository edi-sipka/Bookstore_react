/* eslint-disable */
import React from 'react';

const Categories = () => {
  return (
    <div>
      <button className="{styles.btn}" type="submit">
        Check status
      </button>
    </div>
  );
};

export default Categories;
